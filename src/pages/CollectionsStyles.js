

const styles = (theme) => ({

    pageContainer:{
        minHeight: '90vh',
        padding: '2%',
        width: '100%'
    },
    columns: {
        columnCount: 2,
      }
});

export default styles;